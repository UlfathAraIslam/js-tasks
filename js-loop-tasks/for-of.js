// for of loop is using for arrays elements
const fruits = ['apple','banana'];
for (const fruit of fruits){
    // console.log(fruit);
}
// -------------------------------------------------
const dailyRoutines = ['unlock module at 8 pm', 'watch vedio and practise', 'take notes when watch vedio', 'practise full module when finish the module']

for (const dailyRoutine of dailyRoutines){
    console.log(dailyRoutine);
}
let canUnderstand = false;
if(!canUnderstand){
    console.log('join support session');
}
