//The .includes() method is used to check if a string or an array contains a specific value. It returns true if found and false otherwise.

const books = 'chemistry, math';
console.log(books.includes('math'));